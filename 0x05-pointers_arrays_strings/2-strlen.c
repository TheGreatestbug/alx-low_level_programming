#include "main.h"

/**
 * _strlen - Finds the length of a string
 * @s: a program variable
 * Return: 0
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
	{
		i += 1;
	}
	return (0);
}
